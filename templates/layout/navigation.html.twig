<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-3" aria-label="Navbar">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_index') }}">{% trans %}title{% endtrans %}</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar"
                aria-controls="navbar" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbar">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link nav-link-color" href="{{ path('app_index') }}">
                        {% trans %}navigation.home{% endtrans %}
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link nav-link-color" href="{{ path('app_expenses') }}">
                        {% trans %}navigation.expenses{% endtrans %}
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link nav-link-color" href="{{ path('app_fixed_costs') }}">
                        {% trans %}navigation.fixed_costs{% endtrans %}
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link nav-link-color" href="{{ path('app_overview') }}">
                        {% trans %}navigation.overview{% endtrans %}
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link nav-link-color" href="{{ path('app_categories') }}">
                        {% trans %}navigation.categories{% endtrans %}
                    </a>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="settings-dropdown" data-bs-toggle="dropdown" aria-expanded="false">
                        {% trans %}navigation.settings{% endtrans %}
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="settings-dropdown">
                        <li>
                            <a class="dropdown-item" href="{{ path('app_settings') }}">
                                {% trans %}navigation.change_password{% endtrans %}
                            </a>
                        </li>
                    </ul>
                </li>
            </ul>

            <ul class="navbar-nav me-0 mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link nav-link-color" href="{{ path('app_logout') }}">
                        Logout
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>
